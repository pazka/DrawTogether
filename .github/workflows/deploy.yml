name: build-scan-push
# Phase-2 CI (hosh.it): build -> Trivy gate (CRITICAL) -> push ghcr :ci + :sha.
# Deliberately does NOT touch :latest — the box runs known-good mirrors. Promote a
# validated build with: docker buildx imagetools create -t ghcr.io/pazka/drawtogether:latest ghcr.io/pazka/drawtogether:<sha>
on:
  push:
    branches: [master]
  workflow_dispatch:
permissions:
  contents: read
  packages: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ghcr.io/pazka/drawtogether:candidate
      - name: Trivy gate (fail on CRITICAL)
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/pazka/drawtogether:candidate
          severity: CRITICAL
          exit-code: "1"
          ignore-unfixed: true
      - name: push ci + sha tags
        if: success()
        run: |
          docker tag ghcr.io/pazka/drawtogether:candidate ghcr.io/pazka/drawtogether:ci
          docker tag ghcr.io/pazka/drawtogether:candidate ghcr.io/pazka/drawtogether:${{ github.sha }}
          docker push ghcr.io/pazka/drawtogether:ci
          docker push ghcr.io/pazka/drawtogether:${{ github.sha }}
